{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "apt-get update && apt-get install -y libatomic1 && npm install"
      }
    }
  ]
}
